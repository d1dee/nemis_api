/*
import { Nemis } from "./nemis";

export default (nemisInstance: Nemis) => {
    try {
        if (!(nemisInstance instanceof Nemis)) {
            throw new Error( "Received variable is not an instance of Nemis class")
        }
        const nemis = nemisInstance
        //get instituion details
        // get all learners
        // get all teaching and non teaching staff
        //
        let getAllLearners = Promise.all()
    } catch (err) {
        throw err;
    }
}*/
